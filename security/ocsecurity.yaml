apiVersion: v1
kind: ServiceAccount
metadata:
  # El nombre que usarás para referenciar esta ServiceAccount
  name: cicd-serviceaccount-s
  # Opcional: El namespace donde se creará. Si se omite, se usará 'default'.
  namespace: mauricio-toapanta-dev
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: cicd-binding-s
  namespace: mauricio-toapanta-dev
subjects:
- kind: ServiceAccount
  name: cicd-serviceaccount-s # La cuenta de servicio que creamos
  namespace: mauricio-toapanta-dev
roleRef:
  kind: ClusterRole
  name: edit # El rol que creamos
  apiGroup: rbac.authorization.k8s.io